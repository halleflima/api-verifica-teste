const Pessoas = require('../models/Pessoas')
const Contatos = require('../models/Contatos')
const Enderecos = require('../models/Enderecos')
const Ordens = require('../models/Ordens')

module.exports = {

  async validaPessoa (req, res) {
    try {
      const {}
    } catch (error) {
      
    }
  }

}